# Source DB (already seeded by normal app workflow)
DEMO_DB_NAME=postgres

# Isolated DB for load testing
LOADTEST_DB_NAME=elisei_loadtest

# PostgreSQL credentials for pg_dump/pg_restore
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Password used by demo_seed users for k6 auth flow
DEMO_PASSWORD=Demo123456!
